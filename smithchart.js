function SmithChart (dimension)
{
    this.width = dimension;
    this.height = dimension;
}

SmithChart.prototype.drawOuterRadialAxis = function (axisWidth)
{
    // Draw the outer axis (wavelength scale)
};

SmithChart.prototype.drawInnerRadialAxis = function (axisWidth)
{
    // Draw the inner axis (degree/angle scale)
};